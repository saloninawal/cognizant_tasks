
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;

public class EmployeeDataRetrieval {
    private static final String URL = "jdbc:mysql://localhost:3306/employee_db";
    private static final String USER = "root";
    private static final String PASSWORD = "password";

    public EmployeeDataRetrieval() {
    }

    private static Connection getConnection() throws SQLException {
        Connection var0 = null;

        try {
            Class.forName("com.mysql.cj.jdbc.Driver");
            var0 = DriverManager.getConnection("jdbc:mysql://localhost:3306/employee_db", "root", "password");
        } catch (ClassNotFoundException var2) {
            System.out.println("JDBC Driver not found.");
            var2.printStackTrace();
        } catch (SQLException var3) {
            System.out.println("Error connecting to the database.");
            var3.printStackTrace();
            throw var3;
        }

        return var0;
    }

    public static void getEmployees() {
        String var0 = "SELECT * FROM employees";

        try {
            Connection var1 = getConnection();

            try {
                Statement var2 = var1.createStatement();

                try {
                    ResultSet var3 = var2.executeQuery(var0);

                    try {
                        System.out.printf("%-5s %-20s %-20s %-10s%n", "ID", "Name", "Position", "Salary");

                        while(var3.next()) {
                            int var4 = var3.getInt("id");
                            String var5 = var3.getString("name");
                            String var6 = var3.getString("position");
                            double var7 = var3.getDouble("salary");
                            System.out.printf("%-5d %-20s %-20s %-10.2f%n", var4, var5, var6, var7);
                        }
                    } catch (Throwable var12) {
                        if (var3 != null) {
                            try {
                                var3.close();
                            } catch (Throwable var11) {
                                var12.addSuppressed(var11);
                            }
                        }

                        throw var12;
                    }

                    if (var3 != null) {
                        var3.close();
                    }
                } catch (Throwable var13) {
                    if (var2 != null) {
                        try {
                            var2.close();
                        } catch (Throwable var10) {
                            var13.addSuppressed(var10);
                        }
                    }

                    throw var13;
                }

                if (var2 != null) {
                    var2.close();
                }
            } catch (Throwable var14) {
                if (var1 != null) {
                    try {
                        var1.close();
                    } catch (Throwable var9) {
                        var14.addSuppressed(var9);
                    }
                }

                throw var14;
            }

            if (var1 != null) {
                var1.close();
            }
        } catch (SQLException var15) {
            System.out.println("Error retrieving employee data.");
            var15.printStackTrace();
        }

    }

    public static void main(String[] var0) {
        getEmployees();
    }
}
