
import java.io.IOException;
import java.nio.file.FileVisitResult;
import java.nio.file.Files;
import java.nio.file.LinkOption;
import java.nio.file.Path;
import java.nio.file.SimpleFileVisitor;
import java.nio.file.attribute.BasicFileAttributes;

class DirectoryCopy$1 extends SimpleFileVisitor<Path> {
    DirectoryCopy$1(Path var1, Path var2) {
        this.val$targetDir = var1;
        this.val$sourceDir = var2;
    }

    public FileVisitResult preVisitDirectory(Path var1, BasicFileAttributes var2) throws IOException {
        Path var3 = this.val$targetDir.resolve(this.val$sourceDir.relativize(var1));
        if (!Files.exists(var3, new LinkOption[0])) {
            Files.createDirectory(var3);
        }

        return FileVisitResult.CONTINUE;
    }

    public FileVisitResult visitFile(Path var1, BasicFileAttributes var2) throws IOException {
        Files.copy(var1, this.val$targetDir.resolve(this.val$sourceDir.relativize(var1)));
        return FileVisitResult.CONTINUE;
    }
}
